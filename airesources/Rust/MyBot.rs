mod hlt;

use hlt::Location;
use hlt::GameMapUtils;
use std::io;
use std::collections;
use std::collections::HashMap;
use std::string;
use std::io::Write;
use std::str::FromStr;

static _test_: Vec<u8> = vec!(1);

fn main() {
	println!("Hello world!");
}